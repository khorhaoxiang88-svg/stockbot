# Stockbot public dashboard

Public-safe view of the Stockbot experiment, rankings, selection result, and
pipeline activity. Raw price history and the private SQLite database are never
packaged with this site.

```sh
npm install
npm run dev
```

Edit `app/page.tsx` for dashboard content and `app/globals.css` for presentation.
